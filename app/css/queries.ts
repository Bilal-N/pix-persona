import breakpoints from "./breakpoints";

export const xs = `(max-width: ${breakpoints.xs})px`;
export const s = `(max-width: ${breakpoints.s})px`;
export const m = `(max-width: ${breakpoints.m}px)`;
export const l = `(max-width: ${breakpoints.l}px)`;
export const xl = `(max-width: ${breakpoints.xl}px)`;
export const xxl = `(max-width: ${breakpoints.xxl}px)`;

export default {
  xs,
  s,
  m,
  l,
  xl,
  xxl,
};
